[pytest]
markers =
    integration: testes que batem em API real (custam token, exigem rede)
# A suite rapida nao roda os testes de integracao por padrao.
# Para roda-los de proposito: pytest -m integration
addopts = -m "not integration"
